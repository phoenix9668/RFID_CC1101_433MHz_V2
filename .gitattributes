# Auto detect text files and perform LF normalization
* text=auto

# CAD files are generated data; do not normalize line endings.
*.STEP -text
*.step -text
*.STP -text
*.stp -text

# Documents and reports are binary; never normalize their contents.
*.pdf binary
*.doc binary
*.docx binary
*.xls binary
*.xlsx binary
