# 新电脑 / 新会话启动提示

把下面内容作为新会话的第一条消息；先将项目 clone 并下载 LFS 文件。详细背景在同目录交接文档中，不需要依赖旧聊天记录。

```text
请接续 RFID_CC1101_433MHz_V2 的 V3.8 固件开发。先完整阅读
Doc/Development/SESSION_HANDOFF_20260926.md，核对当前 git status、分支和 LFS，
再读取 RESAMPLING.md、Doc/ADXL362_Resampling_Validation/报告.md 和核心源码。

应从 codex/v38-rebuild-from-34d57c2 分支继续，业务状态至少包含提交
1fbb2580ae0ed49cf7788cf9818d0a23fe4ac6b5，后面还有纯文档交接提交。
不要切到 main，别重新创建 worktree，别合并旧喘息算法。保持一个检出目录、用分支开发。

项目是 STM32L051C8 + ADXL362 + CC1101 的低功耗牛项圈。Keil 已迁为
GNU Arm GCC/CMake，使用 STM32CubeIDE for VS Code。原六类算法、191 字节报文、
CRC、20 分钟上报、12 窗口历史和内部 EEPROM 身份保护都不能随意改变。

实测 ADXL362 25 Hz 档只有约 19.747 Hz，外部参考时钟对照强烈支持内部时基异常。
当前代码已实现方案二：选择 50 Hz 档，RTC/FIFO 估时，整数抗混叠滤波重采样到
25 Hz，再送入原分类器。它仅完成软件测试和历史数据模拟回放，尚未上板。
最后恢复到板上的是旧的正常 STOP、25 Hz 档 Diagnostics，不是当前候选版。

用户门槛：爬跨/活动 >=95%，采食/反刍 >=90%。当前按相对旧算法的召回率和
精确率双门槛评估，没有人工真值。采食合并约 92.96%/93.89%，活动仅11秒达到100%，
爬跨/反刍无覆盖，单文件采食召回89.83%，因此不能宣称全部验收通过。

先在新电脑重新生成本地工具路径/预设，跑主机和Python测试；注意旧三份CSV
不在当前分支目录，不要照搬旧 CsvData 路径。历史文档有25Hz/6秒的旧说明，
当前配置以最新交接、RESAMPLING.md和代码为准。不要覆盖已提交回放证据。

确认 .local 私密备份及精确回退镜像已迁移后，再与我确认板卡/串口/探针状态。
没有确认前不要烧写、复位、擦除EEPROM或连接会改变板卡状态的调试会话。
后续重点是真实50Hz采集/RTC时间验证、类别覆盖、原基站兼容性和生产功耗。
4h20仿真不是4h20实机；tx-ok不是基站收到；万用表直流读数不是纹波测量。

不要上传设备备份、探针信息或个人配置；当我明确要求提交推送时，完成Git和
LFS上传并核对远端，不能只做暂存。请先报告当前环境检查结果与下一步建议。
```
